@model ICollection<Produto>

@{
    Layout = "_Layout";
}

<br />
@if (TempData["MSG_S"] != null)
{
    <br />
    <p class="alert alert-success">@TempData["MSG_S"]</p>

}
else if (TempData["MSG_D"] != null)
{
    <br />
    <p class="alert alert-danger">@TempData["MSG_D"]</p>
}
<br />
@if (Model.Count > 0)
{
    <div class="table-responsive">
        <table class="table table-bordered text-center">
            <thead>
                <tr>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.First().nome_produto)
                    </th>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.First().color)
                    </th>
                    <th scope="col">
                        Status
                    </th>
                    <th scope="col">
                        Operações
                    </th>

                </tr>
            </thead>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.nome_produto)
                    </td>
                    <td>
                        @Convert.ToDateTime(item.data_vencimento).ToString("dd/MM/yyyy")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.color)
                    </td>
                    <td>
                        <a asp-action="Atualizar" asp-route-id="@item.id" class="btn btn-secondary">Atualizar</a>
                        <a asp-action="Detalhes" asp-route-id="@item.id" class="btn btn-info">Detalhes</a>
                        <a asp-action="Excluir" asp-route-id="@item.id" class="btn btn-danger">Excluir</a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div>
    </div>
}
else
{
    <span>Nenhum registro cadastrado</span>

}



@*<td>
    @Html.DisplayFor(modelItem => item.valor, "Currency")
</td>*@